/*
 * @lc app=leetcode.cn id=385 lang=javascript
 * @lcpr version=30204
 *
 * [385] 迷你语法分析器
 */

// @lcpr-template-start

// @lcpr-template-end
// @lc code=start
/**
 * // This is the interface that allows for creating nested lists. 这是允许创建嵌套列表的界面。
 * // You should not implement it, or speculate about its implementation 你不应该实施它，也不应该猜测它的实施
 * function NestedInteger() {
 *
 *     Return true if this NestedInteger holds a single integer, rather than a nested list. 如果此NestedInteger包含单个整数，而不是嵌套列表，则返回true
 *     @return {boolean}
 *     this.isInteger = function() {
 *         ...
 *     };
 *
 *     Return the single integer that this NestedInteger holds, if it holds a single integer 如果NestedInteger只包含一个整数，则返回该整数
 *     Return null if this NestedInteger holds a nested list 如果此NestedInteger包含嵌套列表，则返回null
 *     @return {integer}
 *     this.getInteger = function() {
 *         ...
 *     };
 *
 *     Set this NestedInteger to hold a single integer equal to value. 将此NestedInteger设置为包含一个等于值的整数。
 *     @return {void}
 *     this.setInteger = function(value) {
 *         ...
 *     };
 *
 *     Set this NestedInteger to hold a nested list and adds a nested integer elem to it. 将此NestedInteger设置为包含嵌套列表，并向其中添加嵌套整数elem
 *     @return {void}
 *     this.add = function(elem) {
 *         ...
 *     };
 *
 *     Return the nested list that this NestedInteger holds, if it holds a nested list 如果NestedInteger包含嵌套列表，则返回其包含的嵌套列表
 *     Return null if this NestedInteger holds a single integer 如果此NestedInteger包含单个整数，则返回null
 *     @return {NestedInteger[]}
 *     this.getList = function() {
 *         ...
 *     };
 * };
 */
/**
 * @param {string} s
 * @return {NestedInteger}
 */
var deserialize = function (s) {};
// @lc code=end

/*
// @lcpr case=start
// "324",\n
// @lcpr case=end

// @lcpr case=start
// "[123,[456,[789]]]"\n
// @lcpr case=end

 */
